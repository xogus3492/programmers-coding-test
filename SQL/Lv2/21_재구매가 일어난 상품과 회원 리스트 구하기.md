```sql
SELECT USER_ID, PRODUCT_ID
FROM ONLINE_SALE
GROUP BY USER_ID, PRODUCT_ID
HAVING COUNT(*) >= 2
ORDER BY USER_ID, PRODUCT_ID DESC

=> USER_ID와 PRODUCT_ID 기준으로 그룹화 한것 중 갯수가 2개 이상인 것 
	(HAVING의 역할)
```
